{
  "name": "face-attendance-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "download:model": "node scripts/download-model.mjs",
    "setup": "npm run install:all && npm run download:model && npm run db:setup",
    "db:setup": "npm run db:push --prefix backend && npm run db:seed --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "node backend/dist/index.js",
    "build:prod": "npm run download:model && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
